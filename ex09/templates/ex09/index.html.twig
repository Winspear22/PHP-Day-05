{% extends 'base.html.twig' %}

{% block title %}Ex09 - Database & Entity Management{% endblock %}

{% block body %}
<h1>Ex09 - Database & Entity Management</h1>

{# Flash messages #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}">{{ message|raw }}</div>
    {% endfor %}
{% endfor %}

<hr>
<h2>Étape 1 : Création de la structure de base</h2>
<p>
    Crée la base normalement en lançant :<br>
    <code>php bin/console doctrine:schema:update --force</code>
</p>
<hr>
<h2>Étape 2 : Ajout automatique du champ marital_status</h2>
<form action="{{ path('ex09_update_schema') }}" method="post">
    <button type="submit" class="btn btn-success">
        Injecter automatiquement la propriété marital_status + update DB
    </button>
</form>
<p>
    Ou <a href="{{ path('ex09_show_snippet') }}">voir le code à copier manuellement</a>
    si tu veux faire l'ajout à la main.
</p>
{% endblock %}
